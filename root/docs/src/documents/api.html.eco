---
layout: default
---

<% for section, api of @dox: %>
  <h2><%= section %></h2>

  <% for item in api: %>
    <% if not item.ignore and not item.isPrivate and item.ctx: %>
      <%= item.ctx.name %>
    <% end %>
    <br>
  <% end %>
<% end %>

